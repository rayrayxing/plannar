// Placeholder for TaskList.tsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const TaskList: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Task List (Placeholder)</Typography>
      <Typography>A list of tasks for a project will be displayed here, with options to view/edit individual tasks.</Typography>
    </Box>
  );
};

export default TaskList;
