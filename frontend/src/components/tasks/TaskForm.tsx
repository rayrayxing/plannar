// Placeholder for TaskForm.tsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const TaskForm: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Task Form (Placeholder)</Typography>
      <Typography>Form fields for task details (name, description, status, dates, assignee, etc.) will go here.</Typography>
    </Box>
  );
};

export default TaskForm;
